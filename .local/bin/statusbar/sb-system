#!/bin/sh

homesize=$(df -hP | grep -E "/home$" | awk -F' ' '{print $3" / "$2}')
ram=$(free --mega | awk 'NR==2{printf "%.1f / ", $3/1000; printf "%.1f\n", $2/1000; }')

[ -n "${BLOCK_BUTTON}" ] && case "${BLOCK_BUTTON}" in
    1) ;;
    *) ;;
esac

echo "ram:(${ram}) home:(${homesize})"
